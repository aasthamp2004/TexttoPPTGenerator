# Text to PPT

This project converts text input into a PowerPoint presentation using a Python backend. The main app entry point is `app.py`, while the backend logic and configuration live under `backend/`.

Use the service to submit text and generate slides automatically. Generated presentations are saved in `generated/`, and temporary uploads are stored in `uploads/`.
